import React from 'react';

const ActionHeaderBlock = ({value}) => {
    return (
        <div className="action-screen-header-block d-flex flex-nowrap">
            {value}
        </div>
    );
};

export default ActionHeaderBlock;
